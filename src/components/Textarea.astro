---
type Props = {
	id: string;
	name: string;
};

const { id, name } = Astro.props;
---

<textarea
	class="w-full resize-none rounded-12 border-1 border-primary-500 bg-transparent p-12 font-medium"
	cols={50}
	rows={10}
	name={name}
	id={id}
>
	<slot />
</textarea>
